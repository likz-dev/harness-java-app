server.port=8080

app.name=Sample Java App
app.version=${APP_VERSION:@project.version@}
app.color=${APP_COLOR:blue}

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never
